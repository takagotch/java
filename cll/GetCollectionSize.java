//public int size()

Collection<Integer> col = new ArrayList<>();
for(int i = 0; i < 3; i++){
  col.add(i);
}
System.out.println("COL SIZE: " + col.size());

for(int i = 0; i < 5; i++){
  col.add(i);
}
System.out.println("COL SIZE: " + col.size());

